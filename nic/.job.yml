---
targets:
  - [ "sh", "-c", "make -j$(grep -c processor /proc/cpuinfo) && make gtest && ./run.py" ]
image:
  name: "pensando/nic"
  bind_dir: "/hack/saratk/nic"
  extra_binds:
    /dol: /hack/saratk/dol
rebase: true
