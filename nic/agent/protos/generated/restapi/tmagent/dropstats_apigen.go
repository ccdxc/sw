// Code generated by protoc-gen-grpc-pensando DO NOT EDIT.
/*
 * Package restapi is a auto generated package.
 * Input file: dropstats.proto
 */

package restapi

import (
	"fmt"
	"net/http"

	"github.com/fatih/structs"
	"github.com/gorilla/mux"

	"github.com/pensando/sw/nic/agent/httputils"
	"github.com/pensando/sw/nic/delphi/proto/goproto"
	_ "github.com/pensando/sw/nic/utils/ntranslate/dropstats"
	"github.com/pensando/sw/venice/utils/log"
	"github.com/pensando/sw/venice/utils/tsdb"
)

// AddDropMetricsAPIRoutes adds routes for DropMetrics
func (s *RestServer) AddDropMetricsAPIRoutes(r *mux.Router) {
	r.Methods("GET").Subrouter().HandleFunc("/{Meta.Tenant}/{Meta.Name}/", httputils.MakeHTTPHandler(s.getDropMetricsHandler))
	r.Methods("GET").Subrouter().HandleFunc("/", httputils.MakeHTTPHandler(s.listDropMetricsHandler))
}

// listDropMetricsHandler is the List Handler for DropMetrics
func (s *RestServer) listDropMetricsHandler(r *http.Request) (interface{}, error) {
	iter, err := goproto.NewDropMetricsIterator()
	if err != nil {
		return nil, fmt.Errorf("failed to get metrics, error: %s", err)
	}

	// for OSX tests
	if iter == nil {
		return nil, nil
	}

	var mtr []goproto.DropMetrics

	for iter.HasNext() {
		temp := iter.Next()
		if temp == nil {
			continue
		}

		objMeta := s.keyTranslator.GetObjectMeta("DropMetricsKey", temp.GetKey())
		if objMeta == nil {
			log.Errorf("failed to get objMeta for DropMetrics key %+v", temp.GetKey())
			continue
		}

		temp.ObjectMeta = *objMeta
		mtr = append(mtr, *temp)
	}
	iter.Free()
	return mtr, nil
}

// getDropMetricsPoints returns tags and fields to save in Venice TSDB
func (s *RestServer) getDropMetricsPoints() ([]*tsdb.Point, error) {
	iter, err := goproto.NewDropMetricsIterator()
	if err != nil {
		return nil, fmt.Errorf("failed to get metrics, error: %s", err)
	}

	// for OSX tests
	if iter == nil {
		return nil, nil
	}

	points := []*tsdb.Point{}

	for iter.HasNext() {
		m := iter.Next()
		if m == nil {
			continue
		}

		// translate key to meta
		objMeta := s.keyTranslator.GetObjectMeta("DropMetricsKey", m.GetKey())
		if objMeta == nil {
			log.Errorf("failed to get objMeta for DropMetrics key %+v", m.GetKey())
			continue
		}
		tags := s.getTagsFromMeta(objMeta)
		fields := structs.Map(m)

		if len(fields) > 0 {
			delete(fields, "ObjectMeta")
			points = append(points, &tsdb.Point{Tags: tags, Fields: fields})
		}
	}

	iter.Free()
	return points, nil
}

// getDropMetricsHandler is the Get Handler for DropMetrics
func (s *RestServer) getDropMetricsHandler(r *http.Request) (interface{}, error) {
	log.Infof("Got GET request DropMetrics/%s", mux.Vars(r)["Meta.Name"])
	return nil, nil
}

// AddEgressDropMetricsAPIRoutes adds routes for EgressDropMetrics
func (s *RestServer) AddEgressDropMetricsAPIRoutes(r *mux.Router) {
	r.Methods("GET").Subrouter().HandleFunc("/{Meta.Tenant}/{Meta.Name}/", httputils.MakeHTTPHandler(s.getEgressDropMetricsHandler))
	r.Methods("GET").Subrouter().HandleFunc("/", httputils.MakeHTTPHandler(s.listEgressDropMetricsHandler))
}

// listEgressDropMetricsHandler is the List Handler for EgressDropMetrics
func (s *RestServer) listEgressDropMetricsHandler(r *http.Request) (interface{}, error) {
	iter, err := goproto.NewEgressDropMetricsIterator()
	if err != nil {
		return nil, fmt.Errorf("failed to get metrics, error: %s", err)
	}

	// for OSX tests
	if iter == nil {
		return nil, nil
	}

	var mtr []goproto.EgressDropMetrics

	for iter.HasNext() {
		temp := iter.Next()
		if temp == nil {
			continue
		}

		objMeta := s.keyTranslator.GetObjectMeta("EgressDropMetricsKey", temp.GetKey())
		if objMeta == nil {
			log.Errorf("failed to get objMeta for EgressDropMetrics key %+v", temp.GetKey())
			continue
		}

		temp.ObjectMeta = *objMeta
		mtr = append(mtr, *temp)
	}
	iter.Free()
	return mtr, nil
}

// getEgressDropMetricsPoints returns tags and fields to save in Venice TSDB
func (s *RestServer) getEgressDropMetricsPoints() ([]*tsdb.Point, error) {
	iter, err := goproto.NewEgressDropMetricsIterator()
	if err != nil {
		return nil, fmt.Errorf("failed to get metrics, error: %s", err)
	}

	// for OSX tests
	if iter == nil {
		return nil, nil
	}

	points := []*tsdb.Point{}

	for iter.HasNext() {
		m := iter.Next()
		if m == nil {
			continue
		}

		// translate key to meta
		objMeta := s.keyTranslator.GetObjectMeta("EgressDropMetricsKey", m.GetKey())
		if objMeta == nil {
			log.Errorf("failed to get objMeta for EgressDropMetrics key %+v", m.GetKey())
			continue
		}
		tags := s.getTagsFromMeta(objMeta)
		fields := structs.Map(m)

		if len(fields) > 0 {
			delete(fields, "ObjectMeta")
			points = append(points, &tsdb.Point{Tags: tags, Fields: fields})
		}
	}

	iter.Free()
	return points, nil
}

// getEgressDropMetricsHandler is the Get Handler for EgressDropMetrics
func (s *RestServer) getEgressDropMetricsHandler(r *http.Request) (interface{}, error) {
	log.Infof("Got GET request EgressDropMetrics/%s", mux.Vars(r)["Meta.Name"])
	return nil, nil
}
