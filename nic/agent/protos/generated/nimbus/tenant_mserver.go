// Code generated by protoc-gen-grpc-pensando DO NOT EDIT.

/*
Package nimbus is a auto generated package.
Input file: tenant.proto
*/

package nimbus

import (
	"context"
	"errors"
	"io"
	"strconv"
	"sync"
	"time"

	"github.com/pensando/sw/api"
	"github.com/pensando/sw/nic/agent/protos/netproto"
	hdr "github.com/pensando/sw/venice/utils/histogram"
	"github.com/pensando/sw/venice/utils/log"
	memdb "github.com/pensando/sw/venice/utils/memdb"
	"github.com/pensando/sw/venice/utils/netutils"
	"github.com/pensando/sw/venice/utils/rpckit"
)

// FindTenant finds an Tenant by object meta
func (ms *MbusServer) FindTenant(objmeta *api.ObjectMeta) (*netproto.Tenant, error) {
	// find the object
	obj, err := ms.memDB.FindObject("Tenant", objmeta)
	if err != nil {
		return nil, err
	}

	return TenantFromObj(obj)
}

// ListTenants lists all Tenants in the mbus
func (ms *MbusServer) ListTenants(ctx context.Context, filterFn func(memdb.Object) bool) ([]*netproto.Tenant, error) {
	var objlist []*netproto.Tenant

	// walk all objects
	objs := ms.memDB.ListObjects("Tenant", filterFn)
	for _, oo := range objs {
		obj, err := TenantFromObj(oo)
		if err == nil {
			objlist = append(objlist, obj)
		}
	}

	return objlist, nil
}

// TenantStatusReactor is the reactor interface implemented by controllers
type TenantStatusReactor interface {
	OnTenantCreateReq(nodeID string, objinfo *netproto.Tenant) error
	OnTenantUpdateReq(nodeID string, objinfo *netproto.Tenant) error
	OnTenantDeleteReq(nodeID string, objinfo *netproto.Tenant) error
	OnTenantOperUpdate(nodeID string, objinfo *netproto.Tenant) error
	OnTenantOperDelete(nodeID string, objinfo *netproto.Tenant) error
	GetWatchFilter(kind string, ometa *api.ObjectMeta) func(memdb.Object) bool
}

type TenantNodeStatus struct {
	nodeID        string
	watcher       *memdb.Watcher
	opSentStatus  map[api.EventType]*EventStatus
	opAckedStatus map[api.EventType]*EventStatus
}

// TenantTopic is the Tenant topic on message bus
type TenantTopic struct {
	sync.Mutex
	grpcServer    *rpckit.RPCServer // gRPC server instance
	server        *MbusServer
	statusReactor TenantStatusReactor // status event reactor
	nodeStatus    map[string]*TenantNodeStatus
}

// AddTenantTopic returns a network RPC server
func AddTenantTopic(server *MbusServer, reactor TenantStatusReactor) (*TenantTopic, error) {
	// RPC handler instance
	handler := TenantTopic{
		grpcServer:    server.grpcServer,
		server:        server,
		statusReactor: reactor,
		nodeStatus:    make(map[string]*TenantNodeStatus),
	}

	// register the RPC handlers
	if server.grpcServer != nil {
		netproto.RegisterTenantApiServer(server.grpcServer.GrpcServer, &handler)
	}

	return &handler, nil
}

func (eh *TenantTopic) registerWatcher(nodeID string, watcher *memdb.Watcher) {
	eh.Lock()
	defer eh.Unlock()

	eh.nodeStatus[nodeID] = &TenantNodeStatus{nodeID: nodeID, watcher: watcher}
	eh.nodeStatus[nodeID].opSentStatus = make(map[api.EventType]*EventStatus)
	eh.nodeStatus[nodeID].opAckedStatus = make(map[api.EventType]*EventStatus)
}

func (eh *TenantTopic) unRegisterWatcher(nodeID string) {
	eh.Lock()
	defer eh.Unlock()

	delete(eh.nodeStatus, nodeID)
}

//update recv object status
func (eh *TenantTopic) updateAckedObjStatus(nodeID string, event api.EventType, objMeta *api.ObjectMeta) {

	eh.Lock()
	defer eh.Unlock()
	var evStatus *EventStatus

	nodeStatus, ok := eh.nodeStatus[nodeID]
	if !ok {
		//Watcher already unregistered.
		return
	}

	evStatus, ok = nodeStatus.opAckedStatus[event]
	if !ok {
		nodeStatus.opAckedStatus[event] = &EventStatus{}
		evStatus = nodeStatus.opAckedStatus[event]
	}

	if LatencyMeasurementEnabled {
		rcvdTime, _ := objMeta.ModTime.Time()
		sendTime, _ := objMeta.CreationTime.Time()
		delta := rcvdTime.Sub(sendTime)

		hdr.Record(nodeID+"_"+"Tenant", delta)
		hdr.Record("Tenant", delta)
		hdr.Record(nodeID, delta)
	}

	new, _ := strconv.Atoi(objMeta.ResourceVersion)
	//for create/delete keep track of last one sent to, this may not be full proof
	//  Create could be processed asynchoronusly by client and can come out of order.
	//  For now should be ok as at least we make sure all messages are processed.
	//For update keep track of only last one as nimbus client periodically pulls
	if evStatus.LastObjectMeta != nil {
		current, _ := strconv.Atoi(evStatus.LastObjectMeta.ResourceVersion)
		if current > new {
			return
		}
	}
	evStatus.LastObjectMeta = objMeta
}

//update recv object status
func (eh *TenantTopic) updateSentObjStatus(nodeID string, event api.EventType, objMeta *api.ObjectMeta) {

	eh.Lock()
	defer eh.Unlock()
	var evStatus *EventStatus

	nodeStatus, ok := eh.nodeStatus[nodeID]
	if !ok {
		//Watcher already unregistered.
		return
	}

	evStatus, ok = nodeStatus.opSentStatus[event]
	if !ok {
		nodeStatus.opSentStatus[event] = &EventStatus{}
		evStatus = nodeStatus.opSentStatus[event]
	}

	new, _ := strconv.Atoi(objMeta.ResourceVersion)
	//for create/delete keep track of last one sent to, this may not be full proof
	//  Create could be processed asynchoronusly by client and can come out of order.
	//  For now should be ok as at least we make sure all messages are processed.
	//For update keep track of only last one as nimbus client periodically pulls
	if evStatus.LastObjectMeta != nil {
		current, _ := strconv.Atoi(evStatus.LastObjectMeta.ResourceVersion)
		if current > new {
			return
		}
	}
	evStatus.LastObjectMeta = objMeta
}

//update recv object status
func (eh *TenantTopic) WatcherInConfigSync(nodeID string, event api.EventType) bool {

	var ok bool
	var evStatus *EventStatus
	var evAckStatus *EventStatus

	eh.Lock()
	defer eh.Unlock()

	nodeStatus, ok := eh.nodeStatus[nodeID]
	if !ok {
		return true
	}

	evStatus, ok = nodeStatus.opSentStatus[event]
	if !ok {
		//nothing sent, so insync
		return true
	}

	//In-flight object still exists
	if len(nodeStatus.watcher.Channel) != 0 {
		log.Infof("watcher %v still has objects in in-flight %v(%v)", nodeID, "Tenant", event)
		return false
	}

	evAckStatus, ok = nodeStatus.opAckedStatus[event]
	if !ok {
		//nothing received, failed.
		log.Infof("watcher %v still has not received anything %v(%v)", nodeID, "Tenant", event)
		return false
	}

	if evAckStatus.LastObjectMeta.ResourceVersion < evStatus.LastObjectMeta.ResourceVersion {
		log.Infof("watcher %v resource version mismatch for %v(%v)  sent %v: recived %v",
			nodeID, "Tenant", event, evStatus.LastObjectMeta.ResourceVersion,
			evAckStatus.LastObjectMeta.ResourceVersion)
		return false
	}

	return true
}

/*
//GetSentEventStatus
func (eh *TenantTopic) GetSentEventStatus(nodeID string, event api.EventType) *EventStatus {

    eh.Lock()
    defer eh.Unlock()
    var evStatus *EventStatus

    objStatus, ok := eh.opSentStatus[nodeID]
    if ok {
        evStatus, ok = objStatus.opStatus[event]
        if ok {
            return evStatus
        }
    }
    return nil
}


//GetAckedEventStatus
func (eh *TenantTopic) GetAckedEventStatus(nodeID string, event api.EventType) *EventStatus {

    eh.Lock()
    defer eh.Unlock()
    var evStatus *EventStatus

    objStatus, ok := eh.opAckedStatus[nodeID]
    if ok {
        evStatus, ok = objStatus.opStatus[event]
        if ok {
            return evStatus
        }
    }
    return nil
}

*/

// CreateTenant creates Tenant
func (eh *TenantTopic) CreateTenant(ctx context.Context, objinfo *netproto.Tenant) (*netproto.Tenant, error) {
	nodeID := netutils.GetNodeUUIDFromCtx(ctx)
	log.Infof("Received CreateTenant from node %v: {%+v}", nodeID, objinfo)

	// trigger callbacks. we allow creates to happen before it exists in memdb
	if eh.statusReactor != nil {
		eh.statusReactor.OnTenantCreateReq(nodeID, objinfo)
	}

	// increment stats
	eh.server.Stats("Tenant", "AgentCreate").Inc()

	return objinfo, nil
}

// UpdateTenant updates Tenant
func (eh *TenantTopic) UpdateTenant(ctx context.Context, objinfo *netproto.Tenant) (*netproto.Tenant, error) {
	nodeID := netutils.GetNodeUUIDFromCtx(ctx)
	log.Infof("Received UpdateTenant from node %v: {%+v}", nodeID, objinfo)

	// incr stats
	eh.server.Stats("Tenant", "AgentUpdate").Inc()

	// trigger callbacks
	if eh.statusReactor != nil {
		eh.statusReactor.OnTenantUpdateReq(nodeID, objinfo)
	}

	return objinfo, nil
}

// DeleteTenant deletes an Tenant
func (eh *TenantTopic) DeleteTenant(ctx context.Context, objinfo *netproto.Tenant) (*netproto.Tenant, error) {
	nodeID := netutils.GetNodeUUIDFromCtx(ctx)
	log.Infof("Received DeleteTenant from node %v: {%+v}", nodeID, objinfo)

	// incr stats
	eh.server.Stats("Tenant", "AgentDelete").Inc()

	// trigger callbacks
	if eh.statusReactor != nil {
		eh.statusReactor.OnTenantDeleteReq(nodeID, objinfo)
	}

	return objinfo, nil
}

// TenantFromObj converts memdb object to Tenant
func TenantFromObj(obj memdb.Object) (*netproto.Tenant, error) {
	switch obj.(type) {
	case *netproto.Tenant:
		eobj := obj.(*netproto.Tenant)
		return eobj, nil
	default:
		return nil, ErrIncorrectObjectType
	}
}

// GetTenant returns a specific Tenant
func (eh *TenantTopic) GetTenant(ctx context.Context, objmeta *api.ObjectMeta) (*netproto.Tenant, error) {
	// find the object
	obj, err := eh.server.memDB.FindObject("Tenant", objmeta)
	if err != nil {
		return nil, err
	}

	return TenantFromObj(obj)
}

// ListTenants lists all Tenants matching object selector
func (eh *TenantTopic) ListTenants(ctx context.Context, objsel *api.ObjectMeta) (*netproto.TenantList, error) {
	var objlist netproto.TenantList
	nodeID := netutils.GetNodeUUIDFromCtx(ctx)

	filterFn := func(memdb.Object) bool {
		return true
	}

	if eh.statusReactor != nil {
		filterFn = eh.statusReactor.GetWatchFilter("Tenant", objsel)
	}

	// walk all objects
	objs := eh.server.memDB.ListObjects("Tenant", filterFn)
	//creationTime, _ := types.TimestampProto(time.Now())
	for _, oo := range objs {
		obj, err := TenantFromObj(oo)
		if err == nil {
			//obj.CreationTime = api.Timestamp{Timestamp: *creationTime}
			objlist.Tenants = append(objlist.Tenants, obj)
			//record the last object sent to check config sync
			eh.updateSentObjStatus(nodeID, api.EventType_UpdateEvent, &obj.ObjectMeta)
		}
	}

	return &objlist, nil
}

// WatchTenants watches Tenants and sends streaming resp
func (eh *TenantTopic) WatchTenants(ometa *api.ObjectMeta, stream netproto.TenantApi_WatchTenantsServer) error {
	// watch for changes
	watcher := memdb.Watcher{}
	watcher.Channel = make(chan memdb.Event, memdb.WatchLen)
	defer close(watcher.Channel)

	if eh.statusReactor != nil {
		watcher.Filter = eh.statusReactor.GetWatchFilter("Tenant", ometa)
	} else {
		watcher.Filter = func(memdb.Object) bool {
			return true
		}
	}

	ctx := stream.Context()
	nodeID := netutils.GetNodeUUIDFromCtx(ctx)
	watcher.Name = nodeID
	eh.server.memDB.WatchObjects("Tenant", &watcher)
	defer eh.server.memDB.StopWatchObjects("Tenant", &watcher)

	// get a list of all Tenants
	objlist, err := eh.ListTenants(context.Background(), ometa)
	if err != nil {
		log.Errorf("Error getting a list of objects. Err: %v", err)
		return err
	}

	eh.registerWatcher(nodeID, &watcher)
	defer eh.unRegisterWatcher(nodeID)

	// increment stats
	eh.server.Stats("Tenant", "ActiveWatch").Inc()
	eh.server.Stats("Tenant", "WatchConnect").Inc()
	defer eh.server.Stats("Tenant", "ActiveWatch").Dec()
	defer eh.server.Stats("Tenant", "WatchDisconnect").Inc()

	// walk all Tenants and send it out
	watchEvts := netproto.TenantEventList{}
	for _, obj := range objlist.Tenants {
		watchEvt := netproto.TenantEvent{
			EventType: api.EventType_CreateEvent,
			Tenant:    *obj,
		}
		watchEvts.TenantEvents = append(watchEvts.TenantEvents, &watchEvt)
	}
	if len(watchEvts.TenantEvents) > 0 {
		err = stream.Send(&watchEvts)
		if err != nil {
			log.Errorf("Error sending Tenant to stream. Err: %v", err)
			return err
		}
	}
	timer := time.NewTimer(DefaultWatchHoldInterval)
	if !timer.Stop() {
		<-timer.C
	}

	running := false
	watchEvts = netproto.TenantEventList{}
	sendToStream := func() error {
		err = stream.Send(&watchEvts)
		if err != nil {
			log.Errorf("Error sending Tenant to stream. Err: %v", err)
			return err
		}
		watchEvts = netproto.TenantEventList{}
		return nil
	}

	// loop forever on watch channel
	for {
		select {
		// read from channel
		case evt, ok := <-watcher.Channel:
			if !ok {
				log.Errorf("Error reading from channel. Closing watch")
				return errors.New("Error reading from channel")
			}

			// convert the events
			var etype api.EventType
			switch evt.EventType {
			case memdb.CreateEvent:
				etype = api.EventType_CreateEvent
			case memdb.UpdateEvent:
				etype = api.EventType_UpdateEvent
			case memdb.DeleteEvent:
				etype = api.EventType_DeleteEvent
			}

			// get the object
			obj, err := TenantFromObj(evt.Obj)
			if err != nil {
				return err
			}

			// convert to netproto format
			watchEvt := netproto.TenantEvent{
				EventType: etype,
				Tenant:    *obj,
			}
			watchEvts.TenantEvents = append(watchEvts.TenantEvents, &watchEvt)
			if !running {
				running = true
				timer.Reset(DefaultWatchHoldInterval)
			}
			if len(watchEvts.TenantEvents) >= DefaultWatchBatchSize {
				if err = sendToStream(); err != nil {
					return err
				}
				if !timer.Stop() {
					<-timer.C
				}
				timer.Reset(DefaultWatchHoldInterval)
			}
			eh.updateSentObjStatus(nodeID, etype, &obj.ObjectMeta)
		case <-timer.C:
			running = false
			if err = sendToStream(); err != nil {
				return err
			}
		case <-ctx.Done():
			return ctx.Err()
		}
	}

	// done
}

// updateTenantOper triggers oper update callbacks
func (eh *TenantTopic) updateTenantOper(oper *netproto.TenantEvent, nodeID string) error {
	eh.updateAckedObjStatus(nodeID, oper.EventType, &oper.Tenant.ObjectMeta)
	switch oper.EventType {
	case api.EventType_CreateEvent:
		fallthrough
	case api.EventType_UpdateEvent:
		// incr stats
		eh.server.Stats("Tenant", "AgentUpdate").Inc()

		// trigger callbacks
		if eh.statusReactor != nil {
			return eh.statusReactor.OnTenantOperUpdate(nodeID, &oper.Tenant)
		}
	case api.EventType_DeleteEvent:
		// incr stats
		eh.server.Stats("Tenant", "AgentDelete").Inc()

		// trigger callbacks
		if eh.statusReactor != nil {
			eh.statusReactor.OnTenantOperDelete(nodeID, &oper.Tenant)
		}
	}

	return nil
}

func (eh *TenantTopic) TenantOperUpdate(stream netproto.TenantApi_TenantOperUpdateServer) error {
	ctx := stream.Context()
	nodeID := netutils.GetNodeUUIDFromCtx(ctx)

	for {
		oper, err := stream.Recv()
		if err == io.EOF {
			log.Errorf("%v TenantOperUpdate stream ended. closing..", nodeID)
			return stream.SendAndClose(&api.TypeMeta{})
		} else if err != nil {
			log.Errorf("Error receiving from %v TenantOperUpdate stream. Err: %v", nodeID, err)
			return err
		}

		err = eh.updateTenantOper(oper, nodeID)
		if err != nil {
			log.Errorf("Error updating Tenant oper state. Err: %v", err)
		}
	}
}
