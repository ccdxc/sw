// Code generated by protoc-gen-grpc-pensando DO NOT EDIT.
/*
 * Package restapi is a auto generated package.
 * Input file: pciemgr.proto
 */

package restapi

import (
	"fmt"
	"net/http"

	"github.com/fatih/structs"
	"github.com/gorilla/mux"

	"github.com/pensando/sw/nic/agent/httputils"
	"github.com/pensando/sw/nic/delphi/proto/goproto"
	_ "github.com/pensando/sw/nic/utils/ntranslate/pciemgr"
	"github.com/pensando/sw/venice/utils/log"
	"github.com/pensando/sw/venice/utils/tsdb"
)

// addPcieMgrMetricsAPIRoutes adds routes for PcieMgrMetrics
func addPcieMgrMetricsAPIRoutes(r *mux.Router, srv *RestServer) {
	r.Methods("GET").Subrouter().HandleFunc("/{Meta.Tenant}/{Meta.Name}/", httputils.MakeHTTPHandler(srv.getPcieMgrMetricsHandler))
	r.Methods("GET").Subrouter().HandleFunc("/", httputils.MakeHTTPHandler(srv.listPcieMgrMetricsHandler))
}

// listPcieMgrMetricsHandler is the List Handler for PcieMgrMetrics
func (s *RestServer) listPcieMgrMetricsHandler(r *http.Request) (interface{}, error) {
	iter, err := goproto.NewPcieMgrMetricsIterator()
	if err != nil {
		return nil, fmt.Errorf("failed to get metrics, error: %s", err)
	}

	// for OSX tests
	if iter == nil {
		return nil, nil
	}

	var mtr []goproto.PcieMgrMetrics

	for iter.HasNext() {
		temp := iter.Next()
		if temp == nil {
			continue
		}

		objMeta := s.keyTranslator.GetObjectMeta("PcieMgrMetricsKey", temp.GetKey())
		if objMeta == nil {
			log.Errorf("failed to get objMeta for PcieMgrMetrics key %+v", temp.GetKey())
			continue
		}

		temp.ObjectMeta = *objMeta
		mtr = append(mtr, *temp)
	}
	iter.Free()
	return mtr, nil
}

// getPcieMgrMetricsPoints returns tags and fields to save in Venice TSDB
func (s *RestServer) getPcieMgrMetricsPoints() ([]*tsdb.Point, error) {
	iter, err := goproto.NewPcieMgrMetricsIterator()
	if err != nil {
		return nil, fmt.Errorf("failed to get metrics, error: %s", err)
	}

	// for OSX tests
	if iter == nil {
		return nil, nil
	}

	points := []*tsdb.Point{}

	for iter.HasNext() {
		m := iter.Next()
		if m == nil {
			continue
		}

		// translate key to meta
		objMeta := s.keyTranslator.GetObjectMeta("PcieMgrMetricsKey", m.GetKey())
		if objMeta == nil {
			log.Errorf("failed to get objMeta for PcieMgrMetrics key %+v", m.GetKey())
			continue
		}
		tags := s.getTagsFromMeta(objMeta)
		fields := structs.Map(m)

		if len(fields) > 0 {
			delete(fields, "ObjectMeta")
			points = append(points, &tsdb.Point{Tags: tags, Fields: fields})
		}
	}

	iter.Free()
	return points, nil
}

// getPcieMgrMetricsHandler is the Get Handler for PcieMgrMetrics
func (s *RestServer) getPcieMgrMetricsHandler(r *http.Request) (interface{}, error) {
	log.Infof("Got GET request PcieMgrMetrics/%s", mux.Vars(r)["Meta.Name"])
	return nil, nil
}

// addPciePortMetricsAPIRoutes adds routes for PciePortMetrics
func addPciePortMetricsAPIRoutes(r *mux.Router, srv *RestServer) {
	r.Methods("GET").Subrouter().HandleFunc("/{Meta.Tenant}/{Meta.Name}/", httputils.MakeHTTPHandler(srv.getPciePortMetricsHandler))
	r.Methods("GET").Subrouter().HandleFunc("/", httputils.MakeHTTPHandler(srv.listPciePortMetricsHandler))
}

// listPciePortMetricsHandler is the List Handler for PciePortMetrics
func (s *RestServer) listPciePortMetricsHandler(r *http.Request) (interface{}, error) {
	iter, err := goproto.NewPciePortMetricsIterator()
	if err != nil {
		return nil, fmt.Errorf("failed to get metrics, error: %s", err)
	}

	// for OSX tests
	if iter == nil {
		return nil, nil
	}

	var mtr []goproto.PciePortMetrics

	for iter.HasNext() {
		temp := iter.Next()
		if temp == nil {
			continue
		}

		objMeta := s.keyTranslator.GetObjectMeta("PciePortMetricsKey", temp.GetKey())
		if objMeta == nil {
			log.Errorf("failed to get objMeta for PciePortMetrics key %+v", temp.GetKey())
			continue
		}

		temp.ObjectMeta = *objMeta
		mtr = append(mtr, *temp)
	}
	iter.Free()
	return mtr, nil
}

// getPciePortMetricsPoints returns tags and fields to save in Venice TSDB
func (s *RestServer) getPciePortMetricsPoints() ([]*tsdb.Point, error) {
	iter, err := goproto.NewPciePortMetricsIterator()
	if err != nil {
		return nil, fmt.Errorf("failed to get metrics, error: %s", err)
	}

	// for OSX tests
	if iter == nil {
		return nil, nil
	}

	points := []*tsdb.Point{}

	for iter.HasNext() {
		m := iter.Next()
		if m == nil {
			continue
		}

		// translate key to meta
		objMeta := s.keyTranslator.GetObjectMeta("PciePortMetricsKey", m.GetKey())
		if objMeta == nil {
			log.Errorf("failed to get objMeta for PciePortMetrics key %+v", m.GetKey())
			continue
		}
		tags := s.getTagsFromMeta(objMeta)
		fields := structs.Map(m)

		if len(fields) > 0 {
			delete(fields, "ObjectMeta")
			points = append(points, &tsdb.Point{Tags: tags, Fields: fields})
		}
	}

	iter.Free()
	return points, nil
}

// getPciePortMetricsHandler is the Get Handler for PciePortMetrics
func (s *RestServer) getPciePortMetricsHandler(r *http.Request) (interface{}, error) {
	log.Infof("Got GET request PciePortMetrics/%s", mux.Vars(r)["Meta.Name"])
	return nil, nil
}
