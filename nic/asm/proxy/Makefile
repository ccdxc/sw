

LIBDIRS = \
	  tcp/rx \
	  tcp/tx \
	  tls \
	  tls/pre_crypto \
	  tls/post_crypto \
          $(NULL)
PGMDIRS = $(NULL)
SUBDIRS = $(LIBDIRS) $(PGMDIRS)
TOPDIR = ../..
include $(TOPDIR)/make/Makefile.inc

