

LIBDIRS = \
          nw \
          common-p4+ \
	  proxy/tcp/rx \
          ipsec-p4+ \
          $(NULL)
PGMDIRS = $(NULL)
SUBDIRS = $(LIBDIRS) $(PGMDIRS)
TOPDIR = ..
include $(TOPDIR)/make/Makefile.inc

