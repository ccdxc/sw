

LIBDIRS = \
          nw \
          common-p4+ \
	  proxy \
          rdma \
          ipsec-p4+ \
          storage \
          $(NULL)
PGMDIRS = $(NULL)
SUBDIRS = $(LIBDIRS) $(PGMDIRS)
TOPDIR = ..
include $(TOPDIR)/make/Makefile.inc

