syntax = "proto3";
package metrics;

import "delphi.proto";

message PortCounters {
    delphi.Counter count = 1;
}

message PortMetrics {
    fixed32               Key      = 1;    // Port id
    repeated PortCounters counters = 2;    // Repeated delphi counters for each type
}
