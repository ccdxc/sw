/**
 * This file is generated by the SwaggerTSGenerator.
 * Do not edit.
*/
/* tslint:disable */
import { Validators, FormControl, FormGroup, FormArray, ValidatorFn } from '@angular/forms';
import { minValueValidator, maxValueValidator, minLengthValidator, maxLengthValidator, required, enumValidator, patternValidator, CustomFormControl, CustomFormGroup } from '../../../utils/validators';
import { BaseModel, PropInfoItem } from '../basemodel/base-model';

import { ObjectURIs_ref_type,  } from './enums';
import { ApiObjectRef, IApiObjectRef } from './api-object-ref.model';

export interface IObjectURIs {
    'ref-type': ObjectURIs_ref_type;
    'uri'?: Array<IApiObjectRef>;
    '_ui'?: any;
}


export class ObjectURIs extends BaseModel implements IObjectURIs {
    /** Field for holding arbitrary ui state */
    '_ui': any = {};
    'ref-type': ObjectURIs_ref_type = null;
    'uri': Array<ApiObjectRef> = null;
    public static propInfo: { [prop in keyof IObjectURIs]: PropInfoItem } = {
        'ref-type': {
            enum: ObjectURIs_ref_type,
            default: 'named-reference',
            required: true,
            type: 'string'
        },
        'uri': {
            required: false,
            type: 'object'
        },
    }

    public getPropInfo(propName: string): PropInfoItem {
        return ObjectURIs.propInfo[propName];
    }

    public getPropInfoConfig(): { [key:string]:PropInfoItem } {
        return ObjectURIs.propInfo;
    }

    /**
     * Returns whether or not there is an enum property with a default value
    */
    public static hasDefaultValue(prop) {
        return (ObjectURIs.propInfo[prop] != null &&
                        ObjectURIs.propInfo[prop].default != null);
    }

    /**
     * constructor
     * @param values Can be used to set a webapi response to this newly constructed model
    */
    constructor(values?: any, setDefaults:boolean = true) {
        super();
        this['uri'] = new Array<ApiObjectRef>();
        this._inputValue = values;
        this.setValues(values, setDefaults);
    }

    /**
     * set the values for both the Model and the Form Group. If a value isn't provided and we have a default, we use that.
     * @param values Can be used to set a webapi response to this newly constructed model
    */
    setValues(values: any, fillDefaults = true): void {
        if (values && values['_ui']) {
            this['_ui'] = values['_ui']
        }
        if (values && values['ref-type'] != null) {
            this['ref-type'] = values['ref-type'];
        } else if (fillDefaults && ObjectURIs.hasDefaultValue('ref-type')) {
            this['ref-type'] = <ObjectURIs_ref_type>  ObjectURIs.propInfo['ref-type'].default;
        } else {
            this['ref-type'] = null
        }
        if (values) {
            this.fillModelArray<ApiObjectRef>(this, 'uri', values['uri'], ApiObjectRef);
        } else {
            this['uri'] = [];
        }
        this.setFormGroupValuesToBeModelValues();
    }


    protected getFormGroup(): FormGroup {
        if (!this._formGroup) {
            this._formGroup = new FormGroup({
                'ref-type': CustomFormControl(new FormControl(this['ref-type'], [required, enumValidator(ObjectURIs_ref_type), ]), ObjectURIs.propInfo['ref-type']),
                'uri': new FormArray([]),
            });
            // generate FormArray control elements
            this.fillFormArray<ApiObjectRef>('uri', this['uri'], ApiObjectRef);
            // We force recalculation of controls under a form group
            Object.keys((this._formGroup.get('uri') as FormGroup).controls).forEach(field => {
                const control = this._formGroup.get('uri').get(field);
                control.updateValueAndValidity();
            });
        }
        return this._formGroup;
    }

    setModelToBeFormGroupValues() {
        this.setValues(this.$formGroup.value, false);
    }

    setFormGroupValuesToBeModelValues() {
        if (this._formGroup) {
            this._formGroup.controls['ref-type'].setValue(this['ref-type']);
            this.fillModelArray<ApiObjectRef>(this, 'uri', this['uri'], ApiObjectRef);
        }
    }
}

