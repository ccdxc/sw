/**
 * This file is generated by the SwaggerTSGenerator.
 * Do not edit.
*/
/* tslint:disable */
import { Validators, FormControl, FormGroup, FormArray, ValidatorFn } from '@angular/forms';
import { minValueValidator, maxValueValidator, minLengthValidator, maxLengthValidator, required, enumValidator, patternValidator, CustomFormControl, CustomFormGroup } from '../../../utils/validators';
import { BaseModel, PropInfoItem } from '../basemodel/base-model';

import { ClusterFeatureStatus, IClusterFeatureStatus } from './cluster-feature-status.model';

export interface IClusterLicenseStatus {
    'features'?: Array<IClusterFeatureStatus>;
    'unknown'?: Array<string>;
}


export class ClusterLicenseStatus extends BaseModel implements IClusterLicenseStatus {
    /** Status of current Licenced features */
    'features': Array<ClusterFeatureStatus> = null;
    /** Licenses that are not understood by the current running version of software. */
    'unknown': Array<string> = null;
    public static propInfo: { [prop in keyof IClusterLicenseStatus]: PropInfoItem } = {
        'features': {
            description:  `Status of current Licenced features`,
            required: false,
            type: 'object'
        },
        'unknown': {
            description:  `Licenses that are not understood by the current running version of software.`,
            required: false,
            type: 'Array<string>'
        },
    }

    public getPropInfo(propName: string): PropInfoItem {
        return ClusterLicenseStatus.propInfo[propName];
    }

    public getPropInfoConfig(): { [key:string]:PropInfoItem } {
        return ClusterLicenseStatus.propInfo;
    }

    /**
     * Returns whether or not there is an enum property with a default value
    */
    public static hasDefaultValue(prop) {
        return (ClusterLicenseStatus.propInfo[prop] != null &&
                        ClusterLicenseStatus.propInfo[prop].default != null);
    }

    /**
     * constructor
     * @param values Can be used to set a webapi response to this newly constructed model
    */
    constructor(values?: any, setDefaults:boolean = true) {
        super();
        this['features'] = new Array<ClusterFeatureStatus>();
        this['unknown'] = new Array<string>();
        this._inputValue = values;
        this.setValues(values, setDefaults);
    }

    /**
     * set the values for both the Model and the Form Group. If a value isn't provided and we have a default, we use that.
     * @param values Can be used to set a webapi response to this newly constructed model
    */
    setValues(values: any, fillDefaults = true): void {
        if (values) {
            this.fillModelArray<ClusterFeatureStatus>(this, 'features', values['features'], ClusterFeatureStatus);
        } else {
            this['features'] = [];
        }
        if (values && values['unknown'] != null) {
            this['unknown'] = values['unknown'];
        } else if (fillDefaults && ClusterLicenseStatus.hasDefaultValue('unknown')) {
            this['unknown'] = [ ClusterLicenseStatus.propInfo['unknown'].default];
        } else {
            this['unknown'] = [];
        }
        this.setFormGroupValuesToBeModelValues();
    }


    protected getFormGroup(): FormGroup {
        if (!this._formGroup) {
            this._formGroup = new FormGroup({
                'features': new FormArray([]),
                'unknown': CustomFormControl(new FormControl(this['unknown']), ClusterLicenseStatus.propInfo['unknown']),
            });
            // generate FormArray control elements
            this.fillFormArray<ClusterFeatureStatus>('features', this['features'], ClusterFeatureStatus);
            // We force recalculation of controls under a form group
            Object.keys((this._formGroup.get('features') as FormGroup).controls).forEach(field => {
                const control = this._formGroup.get('features').get(field);
                control.updateValueAndValidity();
            });
        }
        return this._formGroup;
    }

    setModelToBeFormGroupValues() {
        this.setValues(this.$formGroup.value, false);
    }

    setFormGroupValuesToBeModelValues() {
        if (this._formGroup) {
            this.fillModelArray<ClusterFeatureStatus>(this, 'features', this['features'], ClusterFeatureStatus);
            this._formGroup.controls['unknown'].setValue(this['unknown']);
        }
    }
}

