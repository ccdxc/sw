/**
 * This file is generated by the SwaggerTSGenerator.
 * Do not edit.
*/
/* tslint:disable */
import { Validators, FormControl, FormGroup, FormArray, ValidatorFn } from '@angular/forms';
import { minValueValidator, maxValueValidator, minLengthValidator, maxLengthValidator, required, enumValidator, patternValidator, CustomFormControl, CustomFormGroup } from '../../../utils/validators';
import { BaseModel, PropInfoItem } from '../basemodel/base-model';

import { ClusterKeyValue, IClusterKeyValue } from './cluster-key-value.model';

export interface IClusterCredentialsSpec {
    'key-value-pairs'?: Array<IClusterKeyValue>;
    '_ui'?: any;
}


export class ClusterCredentialsSpec extends BaseModel implements IClusterCredentialsSpec {
    /** Field for holding arbitrary ui state */
    '_ui': any = {};
    /** KeyValuePairs contains all key,value pairs that constitute credentials to access a service. */
    'key-value-pairs': Array<ClusterKeyValue> = null;
    public static propInfo: { [prop in keyof IClusterCredentialsSpec]: PropInfoItem } = {
        'key-value-pairs': {
            description:  `KeyValuePairs contains all key,value pairs that constitute credentials to access a service.`,
            required: false,
            type: 'object'
        },
    }

    public getPropInfo(propName: string): PropInfoItem {
        return ClusterCredentialsSpec.propInfo[propName];
    }

    public getPropInfoConfig(): { [key:string]:PropInfoItem } {
        return ClusterCredentialsSpec.propInfo;
    }

    /**
     * Returns whether or not there is an enum property with a default value
    */
    public static hasDefaultValue(prop) {
        return (ClusterCredentialsSpec.propInfo[prop] != null &&
                        ClusterCredentialsSpec.propInfo[prop].default != null);
    }

    /**
     * constructor
     * @param values Can be used to set a webapi response to this newly constructed model
    */
    constructor(values?: any, setDefaults:boolean = true) {
        super();
        this['key-value-pairs'] = new Array<ClusterKeyValue>();
        this._inputValue = values;
        this.setValues(values, setDefaults);
    }

    /**
     * set the values for both the Model and the Form Group. If a value isn't provided and we have a default, we use that.
     * @param values Can be used to set a webapi response to this newly constructed model
    */
    setValues(values: any, fillDefaults = true): void {
        if (values && values['_ui']) {
            this['_ui'] = values['_ui']
        }
        if (values) {
            this.fillModelArray<ClusterKeyValue>(this, 'key-value-pairs', values['key-value-pairs'], ClusterKeyValue);
        } else {
            this['key-value-pairs'] = [];
        }
        this.setFormGroupValuesToBeModelValues();
    }


    protected getFormGroup(): FormGroup {
        if (!this._formGroup) {
            this._formGroup = new FormGroup({
                'key-value-pairs': new FormArray([]),
            });
            // generate FormArray control elements
            this.fillFormArray<ClusterKeyValue>('key-value-pairs', this['key-value-pairs'], ClusterKeyValue);
            // We force recalculation of controls under a form group
            Object.keys((this._formGroup.get('key-value-pairs') as FormGroup).controls).forEach(field => {
                const control = this._formGroup.get('key-value-pairs').get(field);
                control.updateValueAndValidity();
            });
        }
        return this._formGroup;
    }

    setModelToBeFormGroupValues() {
        this.setValues(this.$formGroup.value, false);
    }

    setFormGroupValuesToBeModelValues() {
        if (this._formGroup) {
            this.fillModelArray<ClusterKeyValue>(this, 'key-value-pairs', this['key-value-pairs'], ClusterKeyValue);
        }
    }
}

