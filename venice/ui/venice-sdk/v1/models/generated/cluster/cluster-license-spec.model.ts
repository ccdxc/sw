/**
 * This file is generated by the SwaggerTSGenerator.
 * Do not edit.
*/
/* tslint:disable */
import { Validators, FormControl, FormGroup, FormArray, ValidatorFn } from '@angular/forms';
import { minValueValidator, maxValueValidator, minLengthValidator, maxLengthValidator, required, enumValidator, patternValidator, CustomFormControl, CustomFormGroup } from '../../../utils/validators';
import { BaseModel, PropInfoItem } from '../basemodel/base-model';

import { ClusterFeature, IClusterFeature } from './cluster-feature.model';

export interface IClusterLicenseSpec {
    'features'?: Array<IClusterFeature>;
    '_ui'?: any;
}


export class ClusterLicenseSpec extends BaseModel implements IClusterLicenseSpec {
    /** Field for holding arbitrary ui state */
    '_ui': any = {};
    /** List of Feature licences applied. */
    'features': Array<ClusterFeature> = null;
    public static propInfo: { [prop in keyof IClusterLicenseSpec]: PropInfoItem } = {
        'features': {
            description:  `List of Feature licences applied.`,
            required: false,
            type: 'object'
        },
    }

    public getPropInfo(propName: string): PropInfoItem {
        return ClusterLicenseSpec.propInfo[propName];
    }

    public getPropInfoConfig(): { [key:string]:PropInfoItem } {
        return ClusterLicenseSpec.propInfo;
    }

    /**
     * Returns whether or not there is an enum property with a default value
    */
    public static hasDefaultValue(prop) {
        return (ClusterLicenseSpec.propInfo[prop] != null &&
                        ClusterLicenseSpec.propInfo[prop].default != null);
    }

    /**
     * constructor
     * @param values Can be used to set a webapi response to this newly constructed model
    */
    constructor(values?: any, setDefaults:boolean = true) {
        super();
        this['features'] = new Array<ClusterFeature>();
        this._inputValue = values;
        this.setValues(values, setDefaults);
    }

    /**
     * set the values for both the Model and the Form Group. If a value isn't provided and we have a default, we use that.
     * @param values Can be used to set a webapi response to this newly constructed model
    */
    setValues(values: any, fillDefaults = true): void {
        if (values && values['_ui']) {
            this['_ui'] = values['_ui']
        }
        if (values) {
            this.fillModelArray<ClusterFeature>(this, 'features', values['features'], ClusterFeature);
        } else {
            this['features'] = [];
        }
        this.setFormGroupValuesToBeModelValues();
    }


    protected getFormGroup(): FormGroup {
        if (!this._formGroup) {
            this._formGroup = new FormGroup({
                'features': new FormArray([]),
            });
            // generate FormArray control elements
            this.fillFormArray<ClusterFeature>('features', this['features'], ClusterFeature);
            // We force recalculation of controls under a form group
            Object.keys((this._formGroup.get('features') as FormGroup).controls).forEach(field => {
                const control = this._formGroup.get('features').get(field);
                control.updateValueAndValidity();
            });
        }
        return this._formGroup;
    }

    setModelToBeFormGroupValues() {
        this.setValues(this.$formGroup.value, false);
    }

    setFormGroupValuesToBeModelValues() {
        if (this._formGroup) {
            this.fillModelArray<ClusterFeature>(this, 'features', this['features'], ClusterFeature);
        }
    }
}

