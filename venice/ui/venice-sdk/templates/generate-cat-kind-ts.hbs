{{#each this}}
{{#each this}}
import { {{importName}} } from '{{importPath}}';
{{/each}}
{{/each}}

interface CatMap {
  [key: string]: {
    [key: string]: {
      instance: any,
      scopes: string[],
      actions: string[],
    }
  }
}

export const CategoryMapping: CatMap  = {
  {{#each this}}
  "{{@key}}" : {
    {{#each this}}
    "{{@key}}" : {
      instance: new {{importName}}(),
      scopes: [{{#each scopes}} '{{this}}', {{/each}}] ,
      actions:  [{{#each actions}} '{{this}}', {{/each}}] ,
    },
    {{/each}}
  },
  {{/each}}
}