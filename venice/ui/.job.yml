---
version: 2.0
targets:
  venice-ui:
    commands: [ "sh", "-c", "cd venice/ui/web-app-framework && npm install && npm rebuild node-sass --force && ng test --prod --watch=false --browsers=ChromeHeadlessCI && npm run pack && cd ../venice-sdk && npm install && cd ../webapp &&  npm rebuild node-sass --force && npm install  ../web-app-framework/dist/web-app-framework-0.0.0.tgz && npm install && ng -v && ng test --prod --watch=false  --source-map=false --browsers=ChromeHeadlessCI" ]
    owners: ["email:jeffhu@pensando.io"]
    area:
    sub-area:
    feature:

image:
  bind_dir: "/go/src/github.com/pensando/sw"
  work_dir: "/go/src/github.com/pensando/sw"
rebase: true
