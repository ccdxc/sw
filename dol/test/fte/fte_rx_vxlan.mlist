# Feature Test Modules List
feature:
    id      : fte
    sub     : eth_rx_vxlan
    package : test.eth
    module  : eth
    enable  : True
    ignore  : True
    spec    : rx.testspec
    args    :
        maxflows: 4

modules:
    - module:
        name    : IPV4
        spec    : rx_vxlan.testspec
        iterate:
            - flow      : filter://type=IPV4,proto=UDP,label=FTE,direction=IFLOW
              segment   : filter://native=False
            - flow      : filter://type=IPV4,proto=TCP,label=FTE,direction=IFLOW
              segment   : filter://native=False
            - flow      : filter://type=IPV4,proto=ICMP,label=FTE,direction=IFLOW
              segment   : filter://native=False

    - module:
        name    : IPV6
        spec    : rx_vxlan.testspec
        iterate:
            - flow: filter://type=IPV6,proto=UDP,label=FTE,direction=IFLOW
            - flow: filter://type=IPV6,proto=TCP,label=FTE,direction=IFLOW
            - flow: filter://type=IPV6,proto=ICMPV6,label=FTE,direction=IFLOW

    # - module:
    #     name    : ARP
    #     spec    : rx_vxlan_arp.testspec
    #     iterate :
    #         - type      : ARP_REQ_UC
    #           flow      : filter://direction=IFLOW,type=MAC,label=ARP
    #           segment   : filter://native=False
    #         - type      : ARP_RESP
    #           flow      : filter://direction=IFLOW,type=MAC,label=ARP
    #           segment   : filter://native=False

    # - module:
    #     name    : RARP
    #     spec    : rx_vxlan_arp.testspec
    #     iterate :
    #         - type      : RARP_REQ_UC
    #           flow      : filter://direction=IFLOW,type=MAC,label=ARP
    #           segment   : filter://native=False
    #         - type      : RARP_RESP
    #           segment   : filter://native=False
    #           flow      : filter://direction=IFLOW,type=MAC,label=ARP

    # - module:
    #     name    : NONIP
    #     spec    : rx_vxlan_nonip.testspec
    #     iterate :
    #         - flow: filter://label=FTE,direction=IFLOW,type=MAC
    #           segment   : filter://native=False

    - module:
        name    : L3UC_IPV4
        spec    : rx_vxlan_l3.testspec
        iterate:
            - flow: filter://type=IPV4,proto=UDP,label=FTE,direction=IFLOW
            - flow: filter://type=IPV4,proto=TCP,label=FTE,direction=IFLOW
            - flow: filter://type=IPV4,proto=ICMP,label=FTE,direction=IFLOW

    - module:
        name    : L3UC_IPV6
        spec    : rx_vxlan_l3.testspec
        iterate:
            - flow: filter://type=IPV6,proto=UDP,label=FTE,direction=IFLOW
            - flow: filter://type=IPV6,proto=TCP,label=FTE,direction=IFLOW
            - flow: filter://type=IPV6,proto=ICMPV6,label=FTE,direction=IFLOW
