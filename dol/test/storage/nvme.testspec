# Test Spec
selectors:
    src:
        tenant      : filter://any
        segment     : filter://any
        endpoint    : filter://any
        interface   : filter://any
    dst:
        tenant      : filter://any
        segment     : filter://any
        endpoint    : filter://any
        interface   : filter://any
    flow            : filter://any
    maxflows        : 1

trigger:
expect:
