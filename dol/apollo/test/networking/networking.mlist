# Feature Test Modules List
feature:
    id          : networking
    sub         : l2uc
    package     : test.networking
    module      : networking
    enable      : True
    ignore      : False

modules:
#    - module:
#        name    : IPV4_TCP_QTAG
#        spec    : networking.testspec
#        iterate :
#            - mapping : filter://af=IPV4,label=NETWORKING

    - module:
        name    : IPV6_TCP_QTAG
        spec    : networking.testspec
        iterate :
            - mapping : filter://af=IPV6,label=NETWORKING
