---
targets:
  infra: [ "bash", "-c", "cd /sw/dol && ./sanity.sh" ]
  regr: [ "bash", "-c", "cd /sw/dol && ./regression.sh" ]
cache:
  name: "sw"
  mount_dir: "/root/.cache"
image:
  bind_dir: "/sw"
  work_dir: "/sw/dol"
rebase: true
