---
targets:
  test: [ "sh", "-c", "pwd && ls -l && cd /go/src/github.com/pensando/sw && make" ]
image:
  bind_dir: "/go/src/github.com/pensando/sw"
  work_dir: "/go/src/github.com/pensando/sw"
rebase: true
