---
targets:
  test: [ "sh", "-c", "pwd && ls -l && cd /go/src/github.com/pensando/sw && make jobd-test TO_BUILD=./test/..." ]
image:
  bind_dir: "/go/src/github.com/pensando/sw"
  work_dir: "/go/src/github.com/pensando/sw"
rebase: true
